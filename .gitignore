# machine-local state
.claude/

# dependencies and build output
node_modules/
dist/
dist-electron/
release/
# app icons, regenerated by `npm run icons` (comments must be on their own line —
# an inline one becomes part of the pattern and silently ignores nothing)
build/

# the user's real personal data — never committed (CLAUDE.md §10)
AppLog/
settings.json
