# machine-local state
.claude/

# dependencies and build output
node_modules/
dist/
dist-electron/
release/
build/          # app icons, regenerated by `npm run icons`

# the user's real personal data — never committed (CLAUDE.md §10)
AppLog/
settings.json
